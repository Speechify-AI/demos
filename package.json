{
  "name": "speechify-demos-site",
  "version": "0.0.0",
  "private": true,
  "description": "Speechify demos monorepo. Each folder is a self-contained, clone-and-run demo. The web-hostable demos are also composed into a single Vercel project (Services) served at demos.speechify.ai/<demo-name>.",
  "packageManager": "pnpm@10.9.0",
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
