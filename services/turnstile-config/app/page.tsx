export default function Home() {
  return (
    <main>
      <p>
        Shared Turnstile config endpoint for the demos on demos.speechify.ai.
        See <code>/api/config</code> (mounted at{" "}
        <code>/api/turnstile/config</code> on the public site).
      </p>
    </main>
  );
}
