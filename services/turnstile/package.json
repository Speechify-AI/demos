{
  "name": "turnstile-service",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Turnstile config + verify endpoints for demos.speechify.ai. Fail-open when the env vars are missing so local/fork deploys work without setup.",
  "type": "module"
}
